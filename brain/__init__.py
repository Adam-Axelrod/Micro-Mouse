"""The brain: cells, compass sides and headings. Nothing physical.

The directory IS the rule, as in `sim/`. A module here may import `config` and
other `brain` modules, and nothing else (AGENTS.md invariant 2).
`test_the_layers_hold` enforces it. Empty at import time on purpose.
"""
