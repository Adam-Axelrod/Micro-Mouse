.DS_Store
venv/
.env
__pycache__/
*.pyc

# Run artefacts, one per run, not source. Copy a Pico file in under its own name
# if a particular run is worth keeping.
motor_log.csv
max_speed_test.csv
max_speed_samples.csv
lap_soak.csv
.lavish/

# Working files: the map and the route currently loaded on the robot.
# Committed fixtures live in mazes/ and routes/.
belief.num
route.mmc

# Agent worktrees and local tooling
.claude/
.lavish/
