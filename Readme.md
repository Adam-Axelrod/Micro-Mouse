ok so the mms interaction still works you just have to route to run_mms.py in the old file



to run things you can execute the following while in the Micro-Mouse directory:

python -m micromouse.[python_file]

works for run.py, train.py and eval.py



add the -h flag for help to see what flags you can run it with

python -m micromouse.[python_file] -h

